## CodeBuddy 范围确认

这个代码任务先确认范围：

[1 只诊断]
[2 只读 1-3 文件]
[3 只改一个点]
[4 分阶段重构]

默认建议：`2` 或 `3`

## 测试范围确认

如果需要验证，再选一个：

[1 不跑测试]
[2 最小验证]
[3 相关测试]
[4 完整测试]

默认建议：`2` 或 `3`

## 说明

- `只诊断`：只解释问题，不改代码
- `只读 1-3 文件`：先缩小范围，避免全仓扫描
- `只改一个点`：先交付最小 diff
- `分阶段重构`：先计划，再分批推进
