## 完整版交互式路由确认

```markdown
## 任务路由确认

为了避免误判需求和浪费 token，我先确认 3 件事。你可以直接回复选项编号。

### 1. 这个任务的复杂度是？

A. 简单任务：一次回答即可完成，不涉及重要文件修改  
B. 中等任务：需要 2—3 步处理，可能涉及少量文件或较长文本  
C. 复杂任务：涉及多个文件、知识库、批量处理、项目重构或完整报告  

### 2. 你希望我以哪种模式处理？

A. Ask Mode：只诊断、解释、给建议，不直接修改  
B. Plan Mode：先给计划，等你确认后再执行  
C. Craft Mode：直接生成结果或执行低风险任务  
D. Expert Mode：按某类专家视角处理复杂问题  

### 3. 你希望输出多详细？

A. Micro：只给结论和下一步  
B. Standard：给关键理由和可执行步骤  
C. Deep：系统分析，适合复杂任务  
D. Artifact：生成文件、模板或完整文档  

如果你不确定，建议选择：

```text
复杂度：B
模式：B
详细度：B
```
```

## 极简版提示

```markdown
这个任务可能不止一步。为了省 token，也避免误操作，你选一个方式：

1. 只诊断，不动文件；
2. 先给计划，确认后再执行；
3. 直接做一个最小版本；
4. 召唤专家视角处理。

你也可以回复：`2B`，表示“先给计划 + 标准详细度”。
```

## 按钮友好版极简提示

```markdown
这个任务可能不止一步。你选一个：

[1 只诊断]
[2 先给计划]
[3 先做最小版]
[4 专家视角]

默认建议：`2`
```

## 快捷编码说明

```text
复杂度：S / M / C
模式：A / P / C / E
详细度：M / S / D / F
```

示例：

```text
MPS
```

表示：

```text
中等任务 + Plan Mode + Standard 输出
```

## iMA Copilot 知识库范围确认

```markdown
这看起来是知识库任务。为了节省 token，请选择范围：

1. 只看最相关的 3—5 条材料；
2. 先列材料清单，不展开；
3. 分主题归纳；
4. 生成完整报告。

默认建议：1 或 2。
```

按钮友好版：

```markdown
这看起来是知识库任务，你选一个范围：

[1 只看 3 条]
[2 只列材料]
[3 分主题整理]
[4 完整报告]

默认建议：`1` 或 `2`
```

## WorkBuddy 文件安全确认

```markdown
这个任务可能涉及文件操作。请确认：

1. 只读分析，不改文件；
2. 先生成修改计划；
3. 新建副本，不覆盖原文件；
4. 允许修改指定文件，但需先列清单。

默认建议：2 或 3。
```

按钮友好版：

```markdown
这个任务可能涉及文件操作，请确认：

[1 只读分析]
[2 先给计划]
[3 先做副本]
[4 修改清单后执行]

默认建议：`2` 或 `3`
```

## 高风险组合拦截文案

```markdown
这个任务虽然你选择了 Craft Mode，但它涉及批量知识库整理，属于中高风险操作。为了避免误删、误改和 token 浪费，我需要先进入 Plan Mode，列出处理范围和执行步骤，等你确认后再执行。
```
